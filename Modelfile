# Novel — smartai's custom tool-using agent model, built on an open-source base.
#
# This is the project's own model: the orchestrator. It takes a strong local
# open-source instruct model and bakes in Novel's identity, sampling defaults
# tuned for reliable function/tool calling, and a large context window so the
# self-improving agent loop (maker -> verifier -> memory + skills) has room.
#
# Base: qwen3.5:122b — strong instruct + tool calling, multilingual, and the
# practical RAM sweet spot (~81 GB) for driving days-long sessions. Swap to
# qwen3:235b (HEAVY_MODEL) for max-capability planning, or a smaller model for
# speed, via build_model.sh with SMARTAI_BASE_MODEL set.
#
# Build it:
#   ollama pull qwen3.5:122b         # the open-source base (you already have it)
#   ollama create novel -f Modelfile
#   ollama run novel

FROM qwen3.5:122b

# --- Identity ---------------------------------------------------------------
# Default system prompt for bare `ollama run novel`. The Python agent sends its
# own richer system message per-conversation (with durable memory folded in),
# which takes precedence; this gives the model a sensible identity when used
# directly.
SYSTEM """You are Novel, a capable local AI agent running entirely on the user's own hardware on open-source models — no cloud, no per-token cost.

You can call tools to read and write files, inspect a sandboxed workspace, run shell commands (when enabled), do exact arithmetic, and record what you learn to durable memory. Prefer calling a tool over guessing whenever a tool can give you a precise answer, and pass arguments as valid JSON.

Work in small, verifiable steps. Save durable facts, general rules, and lessons to memory as you discover them so future sessions resume instead of restarting. When you have enough information, stop calling tools and answer directly: lead with the outcome, then any supporting detail. Do not narrate routine actions."""

# --- Sampling defaults ------------------------------------------------------
# Tuned for steady, reproducible tool calling rather than creative writing.
# These are defaults; the agent can still override per-request via the API.
PARAMETER temperature 0.7
PARAMETER top_p 0.9
PARAMETER top_k 40
PARAMETER repeat_penalty 1.05

# Roomy context so multi-step transcripts (tool results + memory + skills + history) fit.
PARAMETER num_ctx 32768

# ChatML turn boundaries — keep generations from running past a turn.
PARAMETER stop "<|im_end|>"
PARAMETER stop "<|im_start|>"
