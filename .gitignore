# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Env / secrets
.env

# Agent workspace (scratch files the agent creates)
workspace/

# Mirror bot local state (which target trades have already been copied)
mirror_state.json
mirror_state.json.tmp

# OS
.DS_Store
